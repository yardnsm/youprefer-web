import styled from 'styled-components';

const Title = styled.h2`
  display: block;

  font-size: 2.5em;
  text-align: center;
  font-weight: 500;

  @media (max-width: 768px) {
    font-size: 1.8em;
  }
`;

export default Title;
